/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Walrus aggregator images are loaded via plain <img>, so no next/image domains needed.
};

export default nextConfig;
